## 项目结构

- `browser-extension/` — 浏览器扩展项目
- `server/` — 配套的 Go 语言实现的轻量后端（Gin + SQLite）
(其他目录不要动)

---

## 需求

帮我实现以下功能，实现后如果涉及到新增功能请更新项目 README (根目录的和当前模块下的)，如果涉及到后端接口变更请更新后端接口文档，完成后请待我确认再提交到本地（注意原子性提交，每个提交应专注一个功能，并记得将你自己加为 Co-author）

### 排期实现需求

**小优化**

feat: 管理后台 - 工作组页面，左侧工作组树的菜单项，添加一个右键 [属性] 菜单，点击后打开一个弹窗，展示当前分组名称、标识色、所在目录（就类似Windows资源管理器那样，以 / 开头，比如 /分组1/子分组2/孙分组3）、描述、创建时间、修改时间、当前工作组标签页数、子工作组数量、子孙工作组标签页总数

refactor: 工作组 - 工作组标签 弹窗感觉交互有些奇怪，能不能按照符合用户使用习惯的方式优化一下这个弹窗

feat: 支持一键关闭所有已经存在于工作组中的已打开标签页（点击[查看已保存的本地标签页]后打开弹窗，显示列表，每项前面有个复选框，默认勾选。点[批量关闭选定标签页]后关闭用户选择的那些页面）

feat: 管理后台 - 工作组 - 分组下标签页前面添加一个复选框，支持批量勾选后，在上方菜单添加跟下面的行右键菜单类似的操作，比如支持批量移动到工作组（弹出那个工作组选择弹窗让用户选择）

---

**中等难度改动**

feat: 目前侧边栏，还有后台管理的几个页面的标签页列表感觉不是很统一（特别是【已同步标签页】这个页面），然后标签页的右键菜单目前只有工作组那里做的比较好，其他的页面包括侧栏，好像都还没有。
列表当时好像抽出来形成了一个组件，但不知道抽的合不合适。然后这个右键菜单，我觉得也可以抽出来形成一个通用的。你帮我评估一下好不好改

feat: 工作组标签寻找更好的展示方式（目前可以设置，但是除了标题旁边，没有地方能够展示出来）

feat: 侧边栏优化内存占用及加载性能 (打开的浏览器页面将近2000个，或许懒加载是否可以解决一部分问题)

---

**大改进**

feat: 兼容Firefox

feat: 支持记录完整导入、导出，部分选择导入导出

spec: 我想将当前这个go后端(目前是看项目，用户可以自部署的形态),新增一个SAAS形态,也就是我提供一个云接口，用户可以选中自部署或者使用我提供的后端。但是如果是我提供的后端，可能就要考虑用户比较多的情况。然后有一些相关的限制，比如每天最多添加25个标签页，最多创建50个分组这种，如果超出的话就需要付费（付费调用我们的另一个主springboot项目接口，付费后带付费结果过来。）现在我就是对这一块儿的改造，心里有点没底。因为改造后同时要保持开源项目用户自部署的功能正常使用，且没有加这些限制，同时SAAS版本要求稳定可靠，考虑并发使用（可能是不是要用MySQL或者PgSQL）

### 待定需求

fix: 现在标签页网页中和右上角的右键菜单，括号中的快捷键是写死的。如果用户编辑了快捷键，那展示的其实就不准了，有没有办法在用户设置了快捷键后这里自适应呢（就是显示用户设置的真实的快捷键）

feat: 标签页自定义显示图标（换图标）

feat: 支持多人使用（后台管理员登录进去，可以配置账号），然后其他人使用账号登录后可以各自创建token来使用

feat: 实现一个简单的 Android App，支持接收浏览器多选分享的多个网址

chore: 处理代码仓库中 Server 端过时的配置文件 (比如 Docker 脚本更新、GitHub Action 脚本、流程简化等)

### 暂缓需求

### 待整理需求

feat: 侧边栏中，窗口/分组折叠菜单，右边都添加一个 [保存新建工作组，并关闭本地标签页] 按钮
feat: 窗口/分组折叠菜单/tab标签页都支持右键复制标题，tab标签页支持右键复制链接、关闭标签页、复制标签页（复制标签页应该跟随那个tab之后打开而不是打开到最后）

feat: 新增一个标签页去重功能（点击后弹出一个弹窗，按组显示重复的标签页列表，然后每组除了时间最早的一个都默认勾选，然后上面给几个按钮：选择日期较新的，选择日期较旧的，选择没有标签的（ i: 若相同组都没有则随机保留一个）。弹窗下面有两个按钮：取消，移除选中项），标签页按域名分组筛选等功能

feat: 支持通过快捷键快速保存并关闭当前标签页
feat: 支持通过网页右键菜单将当前标签页保存到工作组，并关闭标签页

然后现有的标签页tab：改名为本地标签页按照窗口 - [分组] - 标签页 的方式展示（跟侧边栏类似，需考虑下element-plus组件是否好实现）

feat: 本地标签页 选中行上面显示的操作按钮，如果没选中时显示为disabled状态，而不是隐藏，避免按钮显隐切换导致页面布局跳动

refactor: 在工作组中的为标签页添加标签弹窗中，有新建标签页的组件（这里创建时选择颜色好像没效果），标签页也有个专门创建标签的组件，是否可以将创建标签的弹窗拎出来，单独作为一个组件，然后在两个组件中复用？为标签页添加标签弹窗中，如果用户点新建标签按钮，就弹出这个弹窗。如果创建成功，则刷新前一个标签页的列表（大概思路是这样，如果有疑问请先问我再开始）

feat: 工作组tab支持将tab标签页挪到其他工作组下（更进一步，支持拖拽）
feat: 左侧工作组树的菜单项下拉菜单中，添加一个支持移动到目标分组的按钮，点击后弹出刚写的弹窗
我想问一下，这个调整之后的排序有考虑到吗，比如我把一个分组移动到另一个分组下，是否会影响到原分组下标签页的排序？是否能够正确移动到目标分组的最后一项
更进一步，支持拖拽（拖动右边的tab，然后释放到左边的工作组树结构的其中一项上，可以把该标签页记录移动过去，移动过去后应放在目标列表的最后一项），注意不要把其他的逻辑改坏了

---

## 日常维护

docs: 这是一个开源项目，请站在用户的角度来更新项目的 README.md 文档（而不要站在开发者的角度），确保能够在 README.md 中展示出项目的功能（特别是值得一提的亮点功能）、使用方式、安装步骤、常见问题等
注意项目 browser-extension, server 目录下的README.md 也需要更新一下

docs: 帮我看下后端的接口文档页面是否需要更新

我确认后你提交到本地（注意本地还有其他功能改动的代码，你只提交你写的）

请将你的改动提交到本地，注意本地还有其他功能改动的代码，你只提交你写的那些代码。如果改动到同一个文件，你只添加你修改的行到暂存区，不要撤回别人的改动
提交时请使用 cmd 命令行，不要使用 powershell。使用 Windows 系统支持的命令（注意有些命令是只用 Linux 下才能用的）

请整体过一遍当前项目（浏览器扩展 + 前端），检查下
- 项目中的代码设计，架构设计，数据流转，前后端对接交互是否规范、正确，是否存在可能的bug等
- 是否有不再使用的代码，比如重构后遗漏的未使用代码等，如果有，且这些代码上并没有标注未来会使用，或者已明确未来不会使用到，这些代码可以删除掉
- 项目中是否有不规范的代码，比如：
  - 代码中是否有不规范的缩进、空格、换行等
  - 代码中是否有不规范的命名、变量命名、函数命名、文件命名等
  - 代码中是否有不规范的注释、代码注释是否有不规范的格式、内容等
- 代码注释中是否有和AI沟通的痕迹，或者引用了不存在的文件，或者记录了重构的第x步的（如果有，请改为规范的注释表达，不要在打码中标注重构的进度(TODO除外)）
- 已实现不再需要的重构文档、过时的功能文档等请清理掉
- 如果说你发现有其他问题，或者更好的优化点，也请列出

我想请你再帮我系统性地排查一下整个项目，比如：代码分文件、组件封装分层等是否合理，是否有不符合系统设计的 bad smell。然后扩展的相同逻辑，比如前面删除标签页这种，是否是各个地方都统一调用的同一个函数？避免在项目不同的地方同时写多个相同的逻辑，这样未来不好迭代

---

## 常用指令

（注意先详细考虑边界情况再开始实现）

---

## 其他

工作组的选择太不够人性化了，我打开的页面非常多，每次新建工作组就像海里捞针一样
最好能够批量框选标签页，然后加入或新建工作组。也可以看哪些已经加了，哪些还没有加入



对于标签页，我们登录时不进行同步，但是在管理后台，支持查看当前打开的标签页（支持分页查看，筛选等；在浏览器中完成），如果有标签页组，也展示出来
我们可以在上面这个展示中，选择一系列标签页（支持全选），或者通过标签页组批量选择（或者直接选择一个标签页组），来创建一个工作组（这个工作组以及里面的标签页，是保存在后端的）
创建后，我们可以通过我们之前设计的识别标签页的方案，找到对应的那个标签页，把它关掉（但关之前，它已经保存到我们的后端了）
基于以上的使用，用户可能会创建上千个工作组 & 上万个工作组中的页面。我们用一种方便用户操作且美观的UI来展示，支持筛选，支持显示创建时间（工作组创建时间就是数据库记录插入时间，标签页的创建时间是通过 Chrome API 获取的网页打开时间）
用户可以选择打开一个工作组下的所有页面，或者打开某一些/某个页面。（注意打开的这些页面需要特殊标记一下，在前面说到的“支持查看当前打开的标签页”中用tag特殊标出来，每行的所有tag单独到同一列展示）
也就是说，我们主要保存用户选择添加到工作组中的这一批标签页


后台管理-设备tab，当在多个浏览器中配置登录时，并没有出现其他浏览器的信息
后台管理-历史记录tab，改为使用chrome.history API获取，不经过后端
