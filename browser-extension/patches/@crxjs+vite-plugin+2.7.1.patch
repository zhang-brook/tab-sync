diff --git a/node_modules/@crxjs/vite-plugin/dist/index.mjs b/node_modules/@crxjs/vite-plugin/dist/index.mjs
index c998e41..70c9c29 100644
--- a/node_modules/@crxjs/vite-plugin/dist/index.mjs
+++ b/node_modules/@crxjs/vite-plugin/dist/index.mjs
@@ -1975,16 +1975,10 @@ const pluginHMR = () => {
       name: "crx:hmr",
       apply: "serve",
       enforce: "pre",
-      // server hmr host should be localhost
+      // server ws host defaults to localhost in Vite 8, no explicit override needed
       async config({ server = {}, ...config2 }) {
         const opts = await getOptions({ ...config2, server });
         liveReload = opts.liveReload !== false;
-        if (server.hmr === false)
-          return;
-        if (server.hmr === true)
-          server.hmr = {};
-        server.hmr = server.hmr ?? {};
-        server.hmr.host = "localhost";
         return { server };
       },
       // server should ignore outdir
